[project]
name = "books"
version = "1.0.0"
description = "Personal book list CLI – search your library via SQLite"

requires-python = ">=3.11"
license = { text = "MIT" }

dependencies = [
    "click>=8.1",
    "rich>=13.0",
]


